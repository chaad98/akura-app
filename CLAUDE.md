See **[AGENTS.md](./AGENTS.md)** for all project instructions.

That file is the single source of truth for this repo: stack, rendering strategy,
SEO rules, auth handling, and type-safety rules. It is kept in the AGENTS.md format so
every coding agent reads the same instructions.

Also read, in `docs/`:

- `FRONTEND_SPEC.md` — rendering strategy, pages, API contract, SEO, security
- `BACKEND_SPEC.md` — reference copy for endpoints and auth
- `BUILD_PLAN.md` — phase order and exit criteria

Do not duplicate instructions here. Edit `AGENTS.md` instead — two instruction files
drift apart and then contradict each other.
